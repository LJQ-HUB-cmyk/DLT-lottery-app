# 大乐透数据更新指南

## 快速更新流程

每期开奖后（周一、周三、周六），按照以下步骤更新数据：

### 步骤 1：运行快速更新脚本

打开命令提示符或 PowerShell，运行：

```batch
cd e:\lottery-app\lottery-app
.\update-lottery-data.bat
```

**脚本功能**：
- ✅ 自动打开中国体彩网（https://www.lottery.gov.cn/kj/kjlb.html?dlt）
- ✅ 自动打开数据文件（`src\data\lottery-history.txt`）

---

### 步骤 2：复制并粘贴新数据

1. **在浏览器中**：
   - 找到最新一期大乐透开奖号码
   - 复制号码（格式如：`01 12 15 19 26 04 16`）

2. **在记事本中**：
   - 滚动到文件**最后一行**
   - 在最后一行**下面**粘贴新号码（追加到文件末尾）
   - ⚠️ **注意**：不要覆盖已有数据！

3. **保存文件**：
   - 按 `Ctrl+S` 保存
   - 关闭记事本

---

### 步骤 3：提交并推送到 GitHub

在 PowerShell 或命令提示符中执行：

```batch
cd e:\lottery-app
git add -A
git commit -m "feat: 更新大乐透开奖数据至 YYYY-MM-DD"
git push
```

**提交信息示例**：
```
feat: 更新大乐透开奖数据至 2026-05-23
```

**提交后效果**：
-  **网页版用户**：刷新页面即可看到最新数据
- 📱 **APK 用户**：需要重新打包并安装新版本

---

### 步骤 4：重新打包 APK（可选）

如果需要更新 APK 版本：

```batch
cd e:\lottery-app\lottery-app
.\build-android.bat
```

**APK 位置**：
```
e:\lottery-app\lottery-app\android\app\build\outputs\apk\debug\发财大计.apk
```

---

## 数据格式说明

### 标准格式

每行一期数据，7 个数字用空格分隔：

```
前区5个号码 后区2个号码
```

**示例**：
```
01 12 15 19 26 04 16
```

- **前区**：5 个号码（01-35），互不重复
- **后区**：2 个号码（01-12），互不重复

### 数据验证

系统会自动检测以下问题：

❌ **无效数据**（会被跳过）：
- 号码数量不是 7 个
- 前区号码超出 1-35 范围
- 后区号码超出 1-12 范围
- 同一区内有重复号码（如 `06 12 12 21 34 08 09`）

✅ **有效数据**：
- 7 个号码，格式正确
- 前区和后区都无重复
- 号码在有效范围内

---

## 常见问题

### Q1：为什么显示只有 54 组数据？

**原因**：某一行数据存在重复号码，被系统自动跳过了。

**解决方法**：
1. 检查控制台日志，找到被跳过的行
2. 修正重复号码（如 `06 12 12` 改为 `06 12 13`）
3. 保存文件并重新加载

### Q2：新数据应该加在第一行还是最后一行？

**答案**：**最后一行**（追加到文件末尾）。

**原因**：
- 第一行是最旧的数据
- 最后一行是最新的数据
- 系统会从最后一行读取最新一期号码

### Q3：忘记保存文件怎么办？

**解决方法**：
1. 重新打开数据文件
2. 检查是否已添加新数据
3. 如果没有，重新添加并保存
4. 重新提交到 Git

### Q4：Git 推送失败怎么办？

**常见错误**：`OpenSSL SSL_read: Connection was reset`

**解决方法**：
1. 检查网络连接
2. 稍后重试推送
3. 或使用 GitHub Desktop 图形化工具推送

---

## 完整操作示例

### 场景：2026年5月23日大乐透开奖

**开奖号码**：09 10 20 33 35 04 11

**操作流程**：

```powershell
# 1. 运行更新脚本
cd e:\lottery-app\lottery-app
.\update-lottery-data.bat

# 2. 在打开的记事本最后一行添加：
09 10 20 33 35 04 11

# 3. 保存文件（Ctrl+S）

# 4. 提交到 Git
cd e:\lottery-app
git add -A
git commit -m "feat: 更新大乐透开奖数据至 2026-05-23"
git push

# 5. （可选）打包新版本 APK
cd e:\lottery-app\lottery-app
.\build-android.bat
```

---

## 更新频率

大乐透每周开奖 **3 次**：

- **周一** 晚上
- **周三** 晚上
- **周六** 晚上

每次更新约需 **1-2 分钟**。

---

## 文件位置

| 文件 | 路径 | 说明 |
|------|------|------|
| 数据文件 | `lottery-app/src/data/lottery-history.txt` | 存储所有历史开奖数据 |
| 更新脚本 | `lottery-app/update-lottery-data.bat` | 快速更新工具 |
| APK 打包脚本 | `lottery-app/build-android.bat` | 一键打包 APK |

---

## 技术支持

如有问题，请检查：

1. **控制台日志**：打开浏览器按 `F12` 查看是否有错误信息
2. **数据格式**：确保号码格式正确，无重复号码
3. **Git 状态**：运行 `git status` 检查是否有未提交的更改

---

**最后更新**：2026-05-22  
**文档版本**：v1.0
